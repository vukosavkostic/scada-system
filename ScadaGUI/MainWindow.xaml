<Window x:Class="ScadaGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ScadaGUI"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="#efeff1"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="MainWindow" Height="600" Width="800" MinHeight="600" MinWidth="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="65"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            
        </StackPanel>


        <DataGrid Grid.Row="2"  Name ="DataGrid"  SelectedItem="{Binding SelectedProduct}" IsReadOnly="True" SelectionMode="Single" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tag ID" Binding="{Binding Id}" Width="*"/>
                <DataGridTextColumn Header="Address" Binding="{Binding Naziv}" Width="*"/>
                <DataGridTextColumn Header="Value" Binding="{Binding Cena}" Width="*"/>
                <DataGridTextColumn Header="Unit" Binding="{Binding Vrsta}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <Rectangle Grid.Row="1" Fill="#cfd8dc" />
        <Label Grid.Row="1" Content="Tag List" Margin="25, 0, 0, 0" HorizontalAlignment="Center"/>

        <materialDesign:PopupBox
              Style="{StaticResource MaterialDesignMultiFloatingActionPopupBox}"
              PlacementMode="BottomAndAlignCentres"
              ToolTipService.Placement="Right"
              ToolTip="PopupBox, Style MaterialDesignMultiFloatingActionPopupBox"
              Opened="PopupBox_OnOpened"
              Closed="PopupBox_OnClosed">
                        <StackPanel>
                            <Button
                  ToolTip="One with custom opacity"
                  Opacity="0.5"
                  Content="1" />
                            <Button
                  ToolTip="Two"
                  Content="2" />
                            <Button
                  ToolTip="Three"
                  Content="3" />
                        </StackPanel>
        </materialDesign:PopupBox>

        <ToggleButton
          Style="{StaticResource MaterialDesignSwitchToggleButton}"
          HorizontalAlignment="Right"
          Margin="0,20,30,0"
          ToolTip="Default ToggleButton Style" />
        <Label 
            Content="DARK MODE" 
            HorizontalAlignment="Right" 
            Margin="0, 0, 10, 0" 
            FontWeight="Bold" 
            FontSize="13"/>

    </Grid>
</Window>
